<div>
  <h1>Articles</h1>

  Our blog has <%= Article.public_count %> articles and counti
  
  <div>
    <ul class="list-disc">
      <% @articles.each do |article| %>
        <% unless article.archived? %>
          <li>
            <%= link_to article.title, article %>
          </li>
        <% end %>
      <% end %>
    </ul>
  </div>
  <%= link_to "New Article", new_article_path, class:"bg-sky-500 hover:bg-sky-700 px-5 py-2 text-sm leading-5 rounded-full font-semibold text-white" %>
</div>
